# Loaded by serverless-dotenv-plugin at deploy time. Copy to .env and fill in.
# Only non-secret values go here — SUPABASE_SERVICE_ROLE_KEY is NOT read from this
# file, it comes from SSM Parameter Store (see README.md's setup steps).

SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_DOMAIN=flash-mail.vaibhav.rs
